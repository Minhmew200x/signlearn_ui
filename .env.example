VITE_API_BASE_URL=
VITE_PROXY_TARGET=https://api.signlearn.id.vn
VITE_USE_BACKEND_COURSES=true
VITE_GOOGLE_CLIENT_ID=

# Vercel: keep VITE_API_BASE_URL empty so the app uses relative /api
# and lets vercel.json rewrites forward requests to the backend.
